name: Temporary runner probe

on:
  push:
    branches:
      - tmp/pr12-runner-probe

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-22.04
    steps:
      - name: Prove runner execution
        run: |
          echo runner-started
          uname -a
