{
  "schemaVersion": 1,
  "profile": "repository-foundation-v1",
  "tiers": {
    "fast": ["format:check", "lint", "check:capability-budget", "typecheck", "build", "test:unit"],
    "full": [
      "format:check",
      "lint",
      "check:capability-budget",
      "typecheck",
      "build",
      "test:unit",
      "test:e2e",
      "template:smoke"
    ],
    "integration": ["test:e2e"],
    "native-runtime": ["test:native-runtime"],
    "recipes": ["recipes:smoke"],
    "template": ["template:smoke"]
  },
  "requiredCapabilities": [
    "format:check",
    "lint",
    "check:capability-budget",
    "typecheck",
    "build",
    "test:unit"
  ],
  "optionalCapabilities": [
    "test:e2e",
    "test:native-runtime",
    "recipes:smoke",
    "template:smoke"
  ]
}
